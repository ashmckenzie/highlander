<h2>Events</h2>

<h3>Past month</h3>

<%= line_chart(stats.period.events.map { |name, data| { name: name, data: data } }, library: { 'lineWidth' => 1, 'pointSize' => 3 }) %>

<h3>All time</h3>

<%= line_chart(stats.all_time.events.map { |name, data| { name: name, data: data } }, library: { 'lineWidth' => 1, 'pointSize' => 3 }) %>

<h3>Metric breakdown</h3>

<%= pie_chart stats.all_time.events_by_metric, height: '600px' %>
