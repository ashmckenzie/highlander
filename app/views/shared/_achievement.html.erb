<%= link_to image_tag(badge_image_url(achievement.badge)), badge_path(achievement.badge) %>

<div class="badge-details">
  <h3 class="badge-name"><%= achievement.description %></h3>
  <p class="badge-description"><%= achievement.tag %></p>
</div>

<div class="achievement-details">
  <p class="badge-takeup"><%= achievement.badge_takeup %></p>
  <p class="achievement-date"><%= achievement.created_at %> ago</p>
</div>
