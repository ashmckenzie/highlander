<%= image_tag badge_image_url(achievement.badge) %>

<div>
  <h3 class="badge-name"><%= achievement.badge.description %></h3>
  <p class="badge-description"><%= achievement.badge.tag %></p>
  <p class="badge-date"><%= time_ago_in_words(achievement.created_at).capitalize %> ago</p>
</div>
